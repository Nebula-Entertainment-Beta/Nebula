# Audio assets

Place one-shot WAV files here for FMOD / stub playback:

- `hit.wav`
- `jump.wav`
- `objective.wav`
- `ui.wav`

If FMOD is not configured (`FMOD_ROOT`), the engine uses a logging stub that prints play requests without crashing.
