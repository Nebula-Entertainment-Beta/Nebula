/**
 * @file camera3D.h
 * @brief Reserved for a **3D perspective camera** (FPS, third-person, flying cameras).
 *
 * Usually combines a **view** matrix (position + orientation) and a **perspective** projection
 * (field of view, aspect ratio, near/far planes). Games pass the resulting matrices to the vertex
 * shader as uniforms.
 *
 * @note Implementation is not filled in yet — useful when Nebula moves beyond 2D slices.
 */
#pragma once
