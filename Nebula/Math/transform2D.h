/**
 * @file transform2D.h
 * @brief Reserved for **2D transforms**: position, rotation, scale → a 4×4 or 3×3 matrix.
 *
 * Each drawable object often has a transform. Multiplying a sprite’s local corners by the model
 * matrix places it in the world; the camera then maps world space to the screen.
 *
 * @note Implementation is not filled in yet — pair with `camera2D.h` for a full 2D pipeline.
 */
#pragma once
