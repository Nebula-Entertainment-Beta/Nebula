/**
 * @file timestep.h
 * @brief Placeholder for frame delta time (`dt`) and possibly fixed timestep helpers.
 *
 * **Why it matters:** Movement should use `position += velocity * dt` so the game runs at the same
 * speed regardless of FPS. A `Timestep` type makes that explicit and harder to forget.
 */
#pragma once
